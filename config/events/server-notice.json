{
  "id": "server-notice",
  "name": "Server Notice",
  "enabled": true,
  "baseEvent": "any",
  "serverIds": [
    "redacted"
  ],
  "filters": {
    "operator": "AND",
    "filters": [
      {
        "field": "message.content",
        "operator": "matches",
        "pattern": "(connect|disconnect|timeout|error|killed)"
      }
    ]
  },
  "sinkIds": [
    "ntfy"
  ],
  "priority": 600,
  "metadata": {
    "description": "Logs important server notices"
  },
  "__originalId": "server-notice"
}